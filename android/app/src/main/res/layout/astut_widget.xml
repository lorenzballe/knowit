<?xml version="1.0" encoding="utf-8"?>
<!-- The question of the day: an eyebrow, the question set large, and a
     foot with the streak. Dark paper, like the app's cards. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_background"
    android:padding="16dp">

    <TextView
        android:id="@+id/widget_eyebrow"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="ASTUT"
        android:textColor="#99FFFFFF"
        android:textSize="10sp"
        android:letterSpacing="0.14"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/widget_question"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="8dp"
        android:text="Five cards a day."
        android:textColor="#FFFFFFFF"
        android:textSize="17sp"
        android:lineSpacingMultiplier="1.15"
        android:maxLines="4"
        android:ellipsize="end"
        android:fontFamily="serif" />

    <TextView
        android:id="@+id/widget_foot"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="Five cards, two minutes"
        android:textColor="#99FFFFFF"
        android:textSize="12sp"
        android:maxLines="1"
        android:ellipsize="end" />
</LinearLayout>
